[project]
name = "jormungandr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Nejc Drobnic", email = "nejc@nejc.dev" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
jormungandr = "jormungandr:main"

[build-system]
requires = ["uv_build>=0.11.23,<0.12.0"]
build-backend = "uv_build"
